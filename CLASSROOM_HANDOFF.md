# 下一节课从这里继续｜Martin's Monster Quest 3D

更新：2026-09-17（Australia/Sydney）。本文件是独立验收与开发交接，不是产品宣传稿。

## 1. 结论与受测版本
**CHANGES_REQUESTED：基础玩法能试玩，但不能认定为已通过完整通关验收的终极版。**
受测代码提交：`dcf81db4e47c10a98f83180e2d3a615d8f0d795b`（含 9ff5cb1 扩展版）。本轮只增加验收文档、测试证据与接续入口，没有修复游戏代码，也没有启动开发 Worker。
本独立仓库是下一轮开发入口；`student-works` 的同名目录是另一个发布副本，不能拿它的旧文件覆盖本仓库。后续修改需固定新提交再验收，不能继承本报告的通过结论。

## 2. 实测结果
主线使用独立 Chromium 上下文、file:// 入口、真实按钮和键盘。仅固定 `Math.random = () => 0.5` 以便复现，没有注入坐标、HP、攻击、经验或胜利状态。
| 检查 | 结果与证据 |
|---|---|
| 启动、选择角色/伙伴、移动遇敌 | 通过；进入 OVERWORLD，默认路线左3上3遇 Leafbit。 |
| 攻击、血条、捕捉 | 通过；攻击后我方18/30、敌方12/28；CAPTURE成功后队伍从1只变2只。 |
| 战斗外治疗 | 通过；到泉水后两只伙伴分别恢复30/30和28/28。 |
| 打野升级及第一次进化 | 流程可达；Lv.2 Pyrowhisker，60 HP、21攻击；数据一致性有缺陷，见M-01。 |
| 守门者Giga-Golem | 通过此关；四次Ember Blast击败68 HP守门者。 |
| 守门者之后的推荐路线 | 不通过；Lv.2再次进化成Pyrostryke，劲敌战FIGHT菜单只有BACK，没有可用技能。 |
| 战斗输入隔离 | 不通过；方向按钮仍能移动并在战斗中免费回血。 |
| 倒下伙伴切换 | 不通过；单独构造0 HP后备伙伴后，真实SWITCH按钮仍将其派出。此项明确属于边界夹具测试。 |

正确的守门者/后续路线复测编号为M09/M10。初始M07/M08因测试路线走少一步而无效，标为INVALID_HARNESS，不能计入通过项。
未验收通过：劲敌胜利、Stormjaw两阶段完整通关、失败复活后的场景清理、胜利重开、三条起始进化线全覆盖、实体iPad/Safari、长期性能、公开Pages与源码一致性。不能用早先2D版通关记录代替本版。
本次证据目录：[docs/qa/2026-09-17](docs/qa/2026-09-17/)。

## 3. M-01｜P1：提前进化导致主宠没有任何可用攻击
真实复现：默认Flameling→打败Leafbit升级至Lv.2并进化→按任务提示挑战并击败Giga-Golem→确认第二次进化→北上进入劲敌战→打开FIGHT。
实际：Pyrowhisker再次变成Pyrostryke，但仍是Lv.2；新技能最低等级依次为3/4/5/6，菜单只剩BACK。队伍尚有低级后备伙伴，所以不夸称为任何状态都无法恢复的全局死锁；但主宠正常战斗路线已经中断。
定位：`game.js:2338 checkEvolutionOrPerk`、`2347 triggerStage2Evolution`、`1899 updateBattleUI`。`level>=2 && evolvesTo && !rivalDefeated`没有限制进化阶段；守门者奖励又调用相同判断。
同时实测species已为Pyrostryke，但id仍为flameling、stage仍为1、type仍为Fire。进化必须保持这些字段一致。
修复验收：区分自然二阶进化与剧情Mega；任何合法进化后至少有一个当前等级可用招式；id/species/type/stage同步；三种初始伙伴各验证低等级路线。不能靠默认加等级掩盖问题。

## 4. M-02｜P1：战斗移动与免费回血仍未修复
真实复现：默认伙伴左3上3遇Leafbit，攻击一次；保持BATTLE，点屏幕方向键下3次、右8次。
实际：坐标从(-3,0,3)到(5,0,6)，HP从18/30恢复30/30，敌方仍为12/28，没有消费新回合。
定位：`game.js:1668 stepPlayer`只检查cooldown；setupDOM中的D-Pad直接调用它。triggerEncounter及延迟回调也缺少统一锁定和去重。
修复验收：键盘、D-Pad、地面点击共用状态门禁；非OVERWORLD不能通过地图输入移动、治疗或开启新遭遇；切入战斗期间锁定；旧回调不能修改另一场战斗。

## 5. M-03｜P1：SWITCH仍可派出0 HP伙伴
方法：独立测试页构造“一只存活、一只0 HP”的队伍，再点击真实SWITCH。实际activeCreatureIndex=1、当前HP=0、状态仍为BATTLE。
定位：`game.js:2139 handleBattleSwitch`及updateBattleUI/setActionsDisabled的禁用判断。
修复验收：循环只找其他HP>0伙伴，无候选时禁用且不消耗回合；函数和按钮都检查；覆盖1活1倒、2活1倒、全倒及忙碌期间重复点击。

## 6. M-04｜P2：战斗场景已改善，但仍有裁切和遮挡
本次实图确认已有真正的双方3D怪物、阵台、固定镜头，不再沿用旧版“看不到双方怪物”的结论。
仍可见训练师在左侧被裁切，任务条/HUD与敌方信息卡重叠；大型Pyrostryke会遮挡对手。README的“零任何模型遮挡”不等于验收事实。
修复验收：按模型包围盒和HUD安全区取景；横竖屏分别检查小型对小型、大型对大型与Stormjaw，保持关键轮廓与血条可见。

## 7. 源码审查待补测（不冒充已复现）
- updateBattleUI只显示前三个可用技能；部分形态定义四个技能，需验证高等级终结技可达，或明确三槽装备规则。
- handleBattleCapture只拒绝isBoss，未拒绝inEvolutionBattle；需验证捕捉劲敌伙伴后章节标记和后续战斗归属。
- openPerkChoiceModal在全部Perk学完后没有空列表退出分支；需验证再次升级是否卡住。
- 当前没有持久存档实现。是否新增由老师决定；刷新续玩不是本轮已经交付的能力。

## 8. 下一节课执行顺序
1. 先读本文件，再读README和DEVLOG，核对当前分支、HEAD与未提交改动。DEVLOG完成描述不代替测试。
2. 优先修M-01，再统一修M-02/M-03；保留现有三区域、角色和课堂设计，不重建、不扩玩法。
3. 重跑“新建→打野/捕捉→合法进化→治疗→Golem→劲敌→Mega→Stormjaw两阶段→胜利→重开”，以及失败复活流程。
4. 再处理M-04与源码待补测项。交回修改清单、提交SHA、复现前后结果、真实截图；区分固定随机、夹具和普通游玩。
5. 长任务一次领取，完成后交回回执，不做秒级高频监督轮询。

## 9. 启动与交接口令
在仓库根目录执行：
```sh
python3 -m http.server 8888
```
Mac使用`http://127.0.0.1:8888/`；iPad使用当前Mac局域网IP与相同端口。先确认端口归属，不能按端口盲目结束其他服务。本次基础流程还实测了直接双击index.html的file://入口。
本轮未改游戏源码。旧的本地验收文档和早期2D截图保留作历史，不作为当前版本验收通过凭据。

**下节课接续口令：读取本仓库CLASSROOM_HANDOFF.md，核对当前提交，先复现并修复M-01/M-02/M-03，再提交完整回归回执；不要重建游戏，不要把旧版通关描述当成本版通过。**

### 补充：本轮WebKit与证据说明
WebKit 26.0的768×1024触屏配置实际完成标题、选角、探索、D-Pad右移一格；另保存1024×768图。它只证明基础启动/触摸流程，不消除M-01/M-02/M-03，也不等于真实iPad认证。Chromium版本为145.0.7632.6。
仓库证据JSON仅保留有效检查；无效的M07/M08不计入。关键截图包括小型怪物战斗、只有BACK的技能菜单和WebKit横竖屏。当前报告是后续开课的状态入口，早期2D与中途3D验收不覆盖此提交。
